var numbers = [45, 56, 34, 56, 23, 48];
// USE PUSH to add element  an arrary as the last array
//numbers.push(63);
numbers.pop();

// pop the 1st element in array
numbers.shift();
console.log(numbers);

// var element = numbers.pop();
// console.log(element);

var friends = ['balam', 'kalam', 'dalam']
friends.push('jalam');
friends.push('palam');

// push the 1st element in array
friends.unshift('alam');
console.log(friends);

// USE POP to get last element